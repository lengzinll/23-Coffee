services:
  web:
    build:
      context: .
    image: lengzin/23coffee:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      - JWT_SECRET=lengzin23coffee2026
      - DATABASE_URL=file:/app/db.sqlite
    volumes:
      - ~/Desktop/db_management/db.sqlite:/app/db.sqlite
